--- /Users/vian/.codex/worktrees/b5f9/segfault/examples/helper_map.c
+++ generated.c
@@ -1,7 +1,9 @@
 #include "runtime.h"
 static int apply(int value) { return value * 3 + 1; }
 static void kernel(const int * restrict input, int * restrict output, int n) {
-    for (int i = 0; i < n; ++i) {
+    
+#pragma omp parallel for default(none) shared(input, n, output)
+for (int i = 0; i < n; ++i) {
         output[i] = apply(input[i]);
     }
 }
