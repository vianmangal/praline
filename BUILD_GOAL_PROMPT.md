# Prompt for the project build

Paste the text below into the task that will implement Praline. Set your desired model and goal mode in the app. This file does not start an autonomous goal by itself.

---

Build Praline in this repository today, following PRD.md, IMPLEMENTATION_PLAN.md, and SUBMISSION_PLAN.md. The outcome is a runnable P05 hackathon prototype and prepared submission materials: repository contents ready to publish, a demo video or recording-ready procedure if capture is unavailable, a simple slide deck, and a short abstract. Record actual completion states; do not invent public links.

Read repository instructions, preserve existing work, inspect the environment, and begin implementation. Use the chosen Python/Clang AST JSON/OpenMP/static HTML architecture. Make a complete helper-map path work early, then expand only as time allows. Resolve routine implementation choices autonomously.

Implement real AST extraction, declaration-aware helper effects and argument substitution, conservative loop independence and alias checks, CPU transformation, GPU target generation with mapping and device helpers, compile/validate/benchmark harnesses, and a plain evidence report. Safe, unsafe, and unknown must be distinct. Never treat helper purity or passing sample tests as sufficient proof of arbitrary loop safety.

Test positive and negative fixtures, actual generated compile/run, source-edit correctness, stale analysis, and validation failures. Every benchmark needs raw timing samples and environment metadata. GPU results require actual device evidence and mandatory offload. If hardware is unavailable, keep progressing on independent work and report the limitation accurately.

Keep docs/BUILD_LOG.md updated with gates, decisions, commands, tests, measurements, and remaining work. Reserve time for submission. Prefer completing tested features to adding unsupported syntax. Use appropriate document/presentation skills when producing final slide artifacts and inspect renders.

Prepare submission/abstract.md, submission/deck.pdf (and editable deck if feasible), submission/video-script.md, actual screenshots, an evidence bundle, and submission/MANIFEST.md. Attempt video recording only through available authorized tools; if it is unavailable, provide an exact recording checklist and mark the video incomplete. Final claims and slides must match actual implementation and measurements.

Do not create paid resources, send messages, fabricate evidence, or publish/upload without the user's destination and authorization. Do not commit or create branches unless requested. Repository creation/publication can be completed when authorization and account context are supplied; otherwise give the exact local status and next action.

Persist through implementation and verification. Completion means the runnable main path is tested, artifacts are inspected, and the manifest truthfully states each submission item's status. If an external dependency remains unresolved, finish everything independent of it and report the specific missing input. Do not claim a missing video or unverified GPU run is complete.

---
